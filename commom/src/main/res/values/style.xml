<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 自定义弹窗样式 -->
    <style name="BaseDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowAnimationStyle">@style/DialogAnim</item>

    </style>

    <style name="DialogAnim" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/common_enter_from_bottom</item>
        <item name="android:windowExitAnimation">@anim/common_exit_from_bottom</item>
    </style>


</resources>